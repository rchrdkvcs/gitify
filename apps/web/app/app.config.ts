export default defineAppConfig({
  ui: {
    colors: {
      primary: "orange",
      secondary: "green",
      neutral: "gray",
    },
    button: {
      slots: {
        base: "cursor-pointer",
      },
    },
  },
});
